# NodeJS-Lighthouse-Performance-Test
Basic lighthouse (web vitals) performance testing script which will do multiple runs, outputting results into a CSV including a row for average, min and max values.
